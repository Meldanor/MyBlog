---
import type Post from '../interfaces/post';
import BlogListEntry from './BlogListEntry.astro';
interface Props {
  posts: Post[];
}
const { posts } = Astro.props;
---

<div class="flex flex-col gap-8">
  {posts.map((post: Post) => <BlogListEntry post={post} />)}
</div>
