---
import type Category from '../interfaces/category';
interface Props {
  category: Category;
}

const { category } = Astro.props;
---

<span class="ml-2 bg-green-200 text-green-700 rounded-full px-2 py-1 text-sm"
  >{category.attributes.name}</span
>
